import 'package:flutter/material.dart';

/// Example of screen class
class ExampleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: const Scaffold(
        body: const Placeholder(),
      ),
    );
  }
}
