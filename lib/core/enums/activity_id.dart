/// Possible activities to use as [UserGoal] metrics
enum ActivityId {
  distance,
  movingTime,
  elapsedTime,
  totalElevationGain,
  achievementCount,
  kudosCount,
  commentCount,
  athleteCount,
}
